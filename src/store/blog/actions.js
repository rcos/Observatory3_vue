// import { $GET, $POST, $PUT, $DEL } from '@/store/lib/helpers'

// const API_ROOT = '/api/posts'

// Blog actions
export default {
  fetchCollection ({ commit }) {
  },
  fetchModel ({ commit }) {
  },
  create ({ commit }) {
  },
  update ({ commit }) {
  },
  destroy ({ commit }) {
  }
}
